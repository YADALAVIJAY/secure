spring.application.name=SecureFileShare

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/scm
spring.datasource.username=root
spring.datasource.password=!@#$%12345vIJAY
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# File Upload Configuration
spring.servlet.multipart.max-file-size=2GB
spring.servlet.multipart.max-request-size=2GB
file.upload-dir=uploads
file.keys-dir=keys

# Server Port
server.port=${PORT:8080}


# JWT Configuration (Use a strong secret, e.g. 256-bit hex)
jwt.secret=SecureFileShareJwtSecretKey2026SuperStrongKey!
jwt.expiration=86400000

# File Expiration Configuration
file.expiration.minutes=60
file.cleanup.cron=0 * * * * *

clamav.host=192.168.119.128
clamav.port=3310


